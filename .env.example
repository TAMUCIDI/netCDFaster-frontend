# Application Base Path (for Traefik ingress with path prefix)
# Set at BUILD TIME via --build-arg (optional, defaults to /netcdfaster-frontend)
# Example: docker build --build-arg NEXT_PUBLIC_BASE_PATH=/netcdfaster-frontend ...
# Can be overridden at RUNTIME via docker -e
NEXT_PUBLIC_BASE_PATH=/netcdfaster-frontend

# Backend API Configuration
# Set at BUILD TIME via --build-arg (used during Next.js build)
# Example: docker build --build-arg BACKEND_URL=http://127.0.0.1:5000 ...
# For server-side API calls during build
BACKEND_URL=http://127.0.0.1:5000

# Backend API Configuration (for client-side API calls)
# Set at BUILD TIME via --build-arg (optional, used if BACKEND_URL not set)
# Or set at RUNTIME via docker -e to override
# Example: docker run -e NEXT_PUBLIC_BACKEND_URL=http://10.55.180.150/netcdfaster-backend ...
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:5000

# Environment
NODE_ENV=production

# API Configuration (milliseconds)
API_TIMEOUT=30000

# Maximum file size for upload (bytes, default 50MB)
MAX_FILE_SIZE=52428800